{
  "name": "nextjs-hello-world",
  "version": "0.1.0",
  "main": "server.js",
  "scripts": {
    "dev": "SET NODE_OPTIONS=--openssl-legacy-provider && next dev",
    "build": "SET NODE_OPTIONS=--openssl-legacy-provider && next build",
    "start": "SET NODE_OPTIONS=--openssl-legacy-provider && next start",
    "lint": "SET NODE_OPTIONS=--openssl-legacy-provider && next lint",
    "postinstall": "SET NODE_OPTIONS=--openssl-legacy-provider && next build"
  },
  "dependencies": {
    "@paypal/react-paypal-js": "^7.8.1",
    "@reduxjs/toolkit": "^1.8.5",
    "axios": "^1.0.0",
    "cookie": "^0.5.0",
    "cors": "^2.8.5",
    "mongoose": "^6.6.1",
    "next": "^11.1.4",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "react-redux": "^8.0.4",
    "redux": "^4.2.0"
  },
  "devDependencies": {
    "eslint": "^7.32.0",
    "eslint-config-next": "11.1.2"
  }
}
